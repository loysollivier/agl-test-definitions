metadata:
  name: metadata-name-yocto-ptest
  format: "Lava-Test-Shell Test Definition 1.0"
  description: "Wrapper around the Yocto ptests for LAVA"
run:
  steps:
    - ./common/scripts/lava-test.sh
